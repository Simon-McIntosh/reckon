# lint delta on complete trees (a git-archive extraction of each revision, so
# isort sees the package rather than a hand-picked file set).
# base tree /tmp/reckon-gitshim-review-red at 883a1bb7 ; head tree at 9ab5e097
# counts are `ruff check --no-cache -q --output-format=concise <path> | wc -l`
reckon/worker_git_shim.py                       base 0   head 0
tests/test_git_shim_checks_the_resolved_repo.py base 0   head 0
# ruff format --check on both changed files at head:
2 files already formatted