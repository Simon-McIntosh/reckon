{
    "$defs": {
        "BackendConfig": {
            "additionalProperties": false,
            "description": "One worker backend and the routing knobs that apply to it.",
            "properties": {
                "alias": {
                    "description": "Display label rendered in the fleet pane in place of this backend's model identifier. The spelling belongs beside the model it shortens and is decided by whoever writes the configuration; the schema supplies none.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "as_of": {
                    "description": "Date on which this backend's per-million rates were published. A rate is priced only when dated: one whose `as_of` is older than the declared staleness horizon is returned with its age attached rather than silently priced as current. Optional operator data; a backend declaring rates without a date stays explicitly unpriced.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "auth_check": {
                    "description": "Argument vector run to test whether this backend is authenticated, as an exit status. Optional, and user data: it is how a provider-specific credential check reaches reckon without reckon knowing any provider. Availability probing reports the result and never acts on it.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "budget_check": {
                    "description": "Whether a pre-flight may read this backend's own account-limit surface instead of relying on what earlier runs recorded. Off by default, because a read that has to be asked for cannot happen by accident, and because a backend exposing no such surface reports unknown rather than a guess. It is never a model call and consumes no worker budget.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "budget_group": {
                    "description": "Name shared by backends that draw on the same account quota. A pool is declared, never inferred: identical probe readings or reset times are evidence only that two backends were observed the same way, never that they share a budget, so a backend with no declared group stays ungrouped however alike its siblings read. Optional operator data naming the owner's own pool; the schema supplies none.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "catalog": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/CatalogConfig"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Optional declaration for asking a backend command which models it serves. Availability probing runs the declared argument vector and matches the configured model against its output; the declaration remains provider-neutral."
                },
                "command": {
                    "description": "Executable name or path looked up on PATH for a `cli` backend. User data; the schema never supplies one.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "effort": {
                    "description": "Reasoning-effort level passed to this backend. Free text because each backend defines its own vocabulary, and because an effort ladder must not be fixed by reckon.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "effort_spelling": {
                    "additionalProperties": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/EffortSpelling__identifier_optional"
                            },
                            {
                                "description": "The display suffix rendered for an effort level's word.",
                                "type": "string"
                            },
                            {
                                "type": "null"
                            }
                        ]
                    },
                    "description": "Display suffixes for this backend's effort levels, keyed by the effort word. A declared spelling replaces the derived two-character suffix; an effort with no entry renders its first two characters lowercased. User data; the schema enumerates no effort ladder.",
                    "type": [
                        "object",
                        "null"
                    ]
                },
                "endpoints_document": {
                    "description": "Optional path to a document publishing the endpoints this backend's lane is currently serving, as JSON with an `endpoints` list. Availability probing reads it to report whether the lane can serve rather than only that its launcher exists: a document listing at least one endpoint reports serving, one listing none reports not-serving, and an absent declaration or an unreadable or unparsable document reports unknown. User data naming one host's own publication; the schema supplies no path, and the probe never reads it over the network.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "environment": {
                    "additionalProperties": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/EnvironmentVariable__identifier_optional"
                            },
                            {
                                "description": "Value associated with an environment-variable name.",
                                "type": "string"
                            },
                            {
                                "type": "null"
                            }
                        ]
                    },
                    "description": "Environment variables added when this backend's worker is spawned, keyed by variable name. Values are strings and may reference a variable from the dispatcher's environment using `${NAME}`.",
                    "type": [
                        "object",
                        "null"
                    ]
                },
                "fallback": {
                    "description": "Backend to substitute when this one is held on budget. Declared, never inferred: a backend naming no fallback still refuses a held dispatch exactly as one would with this key absent. The substitution is recorded on the run \u2014 the backend asked for, the backend used and the hold that caused it \u2014 so a calibration slice never attributes a fallback run to the backend the caller named. A fallback that is itself held still refuses; this key does not chain into a search across backends.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "input_modalities": {
                    "description": "Input modalities this backend's model actually receives. Absent means undeclared, never \"every modality\": a lane that does not say it reads images is treated as not reading them, so a profile requiring an image is refused by name rather than routed here and answered from the filename. User data naming the lane's own measured path to the model; the schema supplies no value, and a declaration is a claim about the delivered input rather than about what the attachment flag accepts.",
                    "items": {
                        "$ref": "#/$defs/InputModality"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "input_rate_per_million": {
                    "description": "Public input-token price per million tokens for the model this backend serves, as published on the backend's `as_of` date. Together with the output price it is the basis of a notional per-run cost from measured tokens, and of the quota weight comparison. Optional operator data; absence leaves this backend explicitly unpriced.",
                    "minimum": 0,
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "lane_document": {
                    "description": "Optional path to a document a serving lane publishes, carrying the lane's measured state, its remaining headroom, whether a worker binding was observed, the stamp the reading was taken at, and the shelf life the lane itself suggests the reading stays fresh for. Availability probing reads it to report the lane observation beside the serving verdict: a reading older than its own suggested shelf life reports unknown while keeping the figure and its age, and an absent, unreadable or unparsable document reports unknown with a reason and never raises. User data naming one host's own publication; the schema supplies no path, and the probe never reads it over the network.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "launch": {
                    "$ref": "#/$defs/LaunchMode",
                    "description": "How this backend's workers are started."
                },
                "max_concurrent_runs": {
                    "description": "Maximum live worker runs this backend may carry at once. Dispatch counts the non-terminal live pointers claiming this backend and refuses a new dispatch that would exceed the ceiling, naming the backend, the ceiling, the current count and the occupying run ids. Absent or null means unlimited, the default for every backend that does not declare one.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "metered": {
                    "description": "Whether using this backend consumes a metered provider allowance. Optional operator data; absence leaves the backend's meteredness undeclared rather than inferring it from the backend name or another routing property.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "model": {
                    "description": "Model identifier passed to this backend. User data; free text so that no provider vocabulary is encoded here.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "name": {
                    "description": "Map key for an inlined entry.",
                    "type": "string"
                },
                "output_rate_per_million": {
                    "description": "Public output-token price per million tokens for the model this backend serves, as published on the backend's `as_of` date. Together with the input price it is the basis of a notional per-run cost from measured tokens, and of the quota weight comparison. Optional operator data; absence leaves this backend explicitly unpriced.",
                    "minimum": 0,
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "placement": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/PlacementConfig"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Optional scheduler placement for this backend's workers. A declared placement wraps the launch in the scheduler invocation rather than replacing it, so the resolved command, its environment and its stream paths are the ones that run. Absent means the login-node launch, which is what keeps every backend that declares none behaving exactly as before."
                },
                "sandbox": {
                    "$ref": "#/$defs/SandboxMode",
                    "description": "Filesystem blast radius granted to workers of this backend."
                },
                "session_reuse": {
                    "description": "Whether a finished worker session can be resumed rather than respawned.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "time_budget": {
                    "description": "Wall-clock allowance, written as an integer followed by a unit \u2014 `s`, `m` or `h`. It remains the ceiling that bounds a hang: a process that has stopped producing is only caught by elapsed wall clock, never by a token count.",
                    "pattern": "^[0-9]+[smh]$",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "token_budget": {
                    "description": "Worker allowance denominated in generated output tokens \u2014 the quantity the same task needs regardless of what else the lane is doing, so a slow lane inside its token budget is not an overrun however long it took. Written as a bare integer of output tokens. Cannot bound a hang, so the wall-clock `time_budget` ceiling stays under its own name.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "usable_input_window": {
                    "description": "Maximum input tokens this backend can hold after any output reservation has already been removed. Dispatch compares this declared window with a deterministic estimate of the node's standing instructions and named repository files before creating a worktree. Absence means unbounded, never zero: an unknown ceiling cannot justify refusing work.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                }
            },
            "required": [
                "name"
            ],
            "title": "BackendConfig",
            "type": "object"
        },
        "BackendConfig__identifier_optional": {
            "additionalProperties": false,
            "description": "One worker backend and the routing knobs that apply to it.",
            "properties": {
                "alias": {
                    "description": "Display label rendered in the fleet pane in place of this backend's model identifier. The spelling belongs beside the model it shortens and is decided by whoever writes the configuration; the schema supplies none.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "as_of": {
                    "description": "Date on which this backend's per-million rates were published. A rate is priced only when dated: one whose `as_of` is older than the declared staleness horizon is returned with its age attached rather than silently priced as current. Optional operator data; a backend declaring rates without a date stays explicitly unpriced.",
                    "format": "date",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "auth_check": {
                    "description": "Argument vector run to test whether this backend is authenticated, as an exit status. Optional, and user data: it is how a provider-specific credential check reaches reckon without reckon knowing any provider. Availability probing reports the result and never acts on it.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "budget_check": {
                    "description": "Whether a pre-flight may read this backend's own account-limit surface instead of relying on what earlier runs recorded. Off by default, because a read that has to be asked for cannot happen by accident, and because a backend exposing no such surface reports unknown rather than a guess. It is never a model call and consumes no worker budget.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "budget_group": {
                    "description": "Name shared by backends that draw on the same account quota. A pool is declared, never inferred: identical probe readings or reset times are evidence only that two backends were observed the same way, never that they share a budget, so a backend with no declared group stays ungrouped however alike its siblings read. Optional operator data naming the owner's own pool; the schema supplies none.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "catalog": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/CatalogConfig"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Optional declaration for asking a backend command which models it serves. Availability probing runs the declared argument vector and matches the configured model against its output; the declaration remains provider-neutral."
                },
                "command": {
                    "description": "Executable name or path looked up on PATH for a `cli` backend. User data; the schema never supplies one.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "effort": {
                    "description": "Reasoning-effort level passed to this backend. Free text because each backend defines its own vocabulary, and because an effort ladder must not be fixed by reckon.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "effort_spelling": {
                    "additionalProperties": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/EffortSpelling__identifier_optional"
                            },
                            {
                                "description": "The display suffix rendered for an effort level's word.",
                                "type": "string"
                            },
                            {
                                "type": "null"
                            }
                        ]
                    },
                    "description": "Display suffixes for this backend's effort levels, keyed by the effort word. A declared spelling replaces the derived two-character suffix; an effort with no entry renders its first two characters lowercased. User data; the schema enumerates no effort ladder.",
                    "type": [
                        "object",
                        "null"
                    ]
                },
                "endpoints_document": {
                    "description": "Optional path to a document publishing the endpoints this backend's lane is currently serving, as JSON with an `endpoints` list. Availability probing reads it to report whether the lane can serve rather than only that its launcher exists: a document listing at least one endpoint reports serving, one listing none reports not-serving, and an absent declaration or an unreadable or unparsable document reports unknown. User data naming one host's own publication; the schema supplies no path, and the probe never reads it over the network.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "environment": {
                    "additionalProperties": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/EnvironmentVariable__identifier_optional"
                            },
                            {
                                "description": "Value associated with an environment-variable name.",
                                "type": "string"
                            },
                            {
                                "type": "null"
                            }
                        ]
                    },
                    "description": "Environment variables added when this backend's worker is spawned, keyed by variable name. Values are strings and may reference a variable from the dispatcher's environment using `${NAME}`.",
                    "type": [
                        "object",
                        "null"
                    ]
                },
                "fallback": {
                    "description": "Backend to substitute when this one is held on budget. Declared, never inferred: a backend naming no fallback still refuses a held dispatch exactly as one would with this key absent. The substitution is recorded on the run \u2014 the backend asked for, the backend used and the hold that caused it \u2014 so a calibration slice never attributes a fallback run to the backend the caller named. A fallback that is itself held still refuses; this key does not chain into a search across backends.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "input_modalities": {
                    "description": "Input modalities this backend's model actually receives. Absent means undeclared, never \"every modality\": a lane that does not say it reads images is treated as not reading them, so a profile requiring an image is refused by name rather than routed here and answered from the filename. User data naming the lane's own measured path to the model; the schema supplies no value, and a declaration is a claim about the delivered input rather than about what the attachment flag accepts.",
                    "items": {
                        "$ref": "#/$defs/InputModality"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "input_rate_per_million": {
                    "description": "Public input-token price per million tokens for the model this backend serves, as published on the backend's `as_of` date. Together with the output price it is the basis of a notional per-run cost from measured tokens, and of the quota weight comparison. Optional operator data; absence leaves this backend explicitly unpriced.",
                    "minimum": 0,
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "lane_document": {
                    "description": "Optional path to a document a serving lane publishes, carrying the lane's measured state, its remaining headroom, whether a worker binding was observed, the stamp the reading was taken at, and the shelf life the lane itself suggests the reading stays fresh for. Availability probing reads it to report the lane observation beside the serving verdict: a reading older than its own suggested shelf life reports unknown while keeping the figure and its age, and an absent, unreadable or unparsable document reports unknown with a reason and never raises. User data naming one host's own publication; the schema supplies no path, and the probe never reads it over the network.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "launch": {
                    "$ref": "#/$defs/LaunchMode",
                    "description": "How this backend's workers are started."
                },
                "max_concurrent_runs": {
                    "description": "Maximum live worker runs this backend may carry at once. Dispatch counts the non-terminal live pointers claiming this backend and refuses a new dispatch that would exceed the ceiling, naming the backend, the ceiling, the current count and the occupying run ids. Absent or null means unlimited, the default for every backend that does not declare one.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "metered": {
                    "description": "Whether using this backend consumes a metered provider allowance. Optional operator data; absence leaves the backend's meteredness undeclared rather than inferring it from the backend name or another routing property.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "model": {
                    "description": "Model identifier passed to this backend. User data; free text so that no provider vocabulary is encoded here.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "name": {
                    "description": "Map key for an inlined entry.",
                    "type": "string"
                },
                "output_rate_per_million": {
                    "description": "Public output-token price per million tokens for the model this backend serves, as published on the backend's `as_of` date. Together with the input price it is the basis of a notional per-run cost from measured tokens, and of the quota weight comparison. Optional operator data; absence leaves this backend explicitly unpriced.",
                    "minimum": 0,
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "placement": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/PlacementConfig"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Optional scheduler placement for this backend's workers. A declared placement wraps the launch in the scheduler invocation rather than replacing it, so the resolved command, its environment and its stream paths are the ones that run. Absent means the login-node launch, which is what keeps every backend that declares none behaving exactly as before."
                },
                "sandbox": {
                    "$ref": "#/$defs/SandboxMode",
                    "description": "Filesystem blast radius granted to workers of this backend."
                },
                "session_reuse": {
                    "description": "Whether a finished worker session can be resumed rather than respawned.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "time_budget": {
                    "description": "Wall-clock allowance, written as an integer followed by a unit \u2014 `s`, `m` or `h`. It remains the ceiling that bounds a hang: a process that has stopped producing is only caught by elapsed wall clock, never by a token count.",
                    "pattern": "^[0-9]+[smh]$",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "token_budget": {
                    "description": "Worker allowance denominated in generated output tokens \u2014 the quantity the same task needs regardless of what else the lane is doing, so a slow lane inside its token budget is not an overrun however long it took. Written as a bare integer of output tokens. Cannot bound a hang, so the wall-clock `time_budget` ceiling stays under its own name.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "usable_input_window": {
                    "description": "Maximum input tokens this backend can hold after any output reservation has already been removed. Dispatch compares this declared window with a deterministic estimate of the node's standing instructions and named repository files before creating a worktree. Absence means unbounded, never zero: an unknown ceiling cannot justify refusing work.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                }
            },
            "required": [],
            "title": "BackendConfig",
            "type": "object"
        },
        "BudgetConfig": {
            "additionalProperties": false,
            "description": "Thresholds that decide whether a wave opens. They are compared against whatever a backend actually reported; a backend that reports nothing is never held by them.",
            "properties": {
                "coordinator_reserve_pct": {
                    "description": "Headroom withheld from new dispatches, in percentage points, so the coordinator that must survive a wave keeps headroom of its own. The reported account position already counts the coordinator's traffic, but reckon cannot see the total; a wave sized to fill the remaining headroom leaves nothing for the audit, merge and outcome-recording the coordinator does after the workers finish, so it can be refused mid-wave with every worker's output stranded in a worktree and nobody left to land it. A fresh dispatch stops at the ceiling less both this reserve and the resume reserve; a resume may spend either, because answering a stuck worker is the expenditure the reserves were withheld for. The figure is declared, not measured: there is no coordinator burn-rate record on this machine, and reading the account surface on a cadence is what calibrates it later.",
                    "maximum": 100,
                    "minimum": 0,
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "drain_lead_hours": {
                    "description": "How long before a group's weekly quota reset its budget should be drained, in hours. The pace deadline is the weekly period less this lead, so the week ends spent rather than exhausted mid-window with work still queued. A longer lead closes the deadline earlier and raises the share each remaining five-hour window is asked for; zero drains exactly at the reset, and a lead at or beyond the weekly period asks the next window for whatever remains.",
                    "minimum": 0,
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "evidence_shelf_life_minutes": {
                    "description": "Minutes a refusal that names no reset time keeps describing the present before it is treated as stale and stops holding the wave on its own. Has no effect on a refusal that names a reset, since a stated reset is stronger evidence than an age and already carries its own expiry. A value at or below zero disables ageing and restores an indefinite hold.",
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "exhausted_statuses": {
                    "description": "Threshold-status values that count as exhausted whatever the utilisation reads \u2014 the overage question, answered as data so that the schema enumerates no backend's vocabulary. Empty leaves the ceiling as the only test.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "pace_multiple": {
                    "description": "Deliberate lean above the linear share, applied on top of the same derivation for every declared budget group. A group exactly on pace returns this multiple times the nominal share of the weekly budget per five-hour window, rather than the bare nominal share, so the week converges on its deadline slightly ahead of it. A group's returned allowance records the multiple that produced it, because the multiple is retuned over a longer horizon than one week.",
                    "minimum": 0,
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "resume_reserve_pct": {
                    "description": "Headroom withheld from new dispatches, in percentage points, so a worker that stops and asks for help can still be answered in its own session. A fresh dispatch stops at the ceiling less this reserve; answering a stuck worker may spend it. Spending the last of a quota on a new dispatch strands the wave in its worst state \u2014 work in flight and no way to unblock it.",
                    "maximum": 100,
                    "minimum": 0,
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "utilisation_ceiling_pct": {
                    "description": "Reported utilisation, as a percentage, at or above which a wave will not open. A backend reporting no headroom is never held by this: absence of a signal is not evidence of exhaustion, and a false hold stalls everything while a rejected call is cheap and announces itself.",
                    "maximum": 100,
                    "minimum": 0,
                    "type": [
                        "number",
                        "null"
                    ]
                }
            },
            "title": "BudgetConfig",
            "type": "object"
        },
        "CatalogConfig": {
            "additionalProperties": false,
            "description": "Provider-neutral model catalog probe owned by a backend.",
            "properties": {
                "list_command": {
                    "description": "Argument vector that prints the models served by this backend. The vector is user data and includes the executable.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "model_pattern": {
                    "description": "Regular expression used against each catalog output line. The required `{model}` placeholder is replaced by the escaped configured model.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "CatalogConfig",
            "type": "object"
        },
        "EffortSpelling": {
            "additionalProperties": false,
            "description": "One effort level and its declared display suffix.",
            "properties": {
                "name": {
                    "description": "Map key for an inlined entry.",
                    "type": "string"
                },
                "spelling": {
                    "description": "The display suffix rendered for an effort level's word.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "name"
            ],
            "title": "EffortSpelling",
            "type": "object"
        },
        "EffortSpelling__identifier_optional": {
            "additionalProperties": false,
            "description": "One effort level and its declared display suffix.",
            "properties": {
                "name": {
                    "description": "Map key for an inlined entry.",
                    "type": "string"
                },
                "spelling": {
                    "description": "The display suffix rendered for an effort level's word.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [],
            "title": "EffortSpelling",
            "type": "object"
        },
        "EnvironmentVariable": {
            "additionalProperties": false,
            "description": "One environment-variable name and its string value.",
            "properties": {
                "name": {
                    "description": "Map key for an inlined entry.",
                    "type": "string"
                },
                "value": {
                    "description": "Value associated with an environment-variable name.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "name"
            ],
            "title": "EnvironmentVariable",
            "type": "object"
        },
        "EnvironmentVariable__identifier_optional": {
            "additionalProperties": false,
            "description": "One environment-variable name and its string value.",
            "properties": {
                "name": {
                    "description": "Map key for an inlined entry.",
                    "type": "string"
                },
                "value": {
                    "description": "Value associated with an environment-variable name.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [],
            "title": "EnvironmentVariable",
            "type": "object"
        },
        "FenceConfig": {
            "additionalProperties": false,
            "description": "Limits a worker applies to itself before asking for help.",
            "properties": {
                "budget_grace_multiple": {
                    "description": "Multiple of a run's declared time budget allowed before opt-in watchdog enforcement stops it. Values below one would stop before the declared allowance elapsed.",
                    "minimum": 1,
                    "type": [
                        "number",
                        "null"
                    ]
                },
                "enforce_budget_watchdog": {
                    "description": "Whether observation stops a live CLI worker after its declared time budget multiplied by the configured grace. Off by default; classification always reports the overrun without mutating the run.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "manifest_required": {
                    "description": "Whether a worker must write its manifest to the orchestrator-named path before its node counts as delivered.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "needs_help_after_failures": {
                    "description": "Consecutive failures after which a worker stops retrying and asks for help. Zero disables the fence.",
                    "minimum": 0,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "time_budget": {
                    "description": "Wall-clock allowance, written as an integer followed by a unit \u2014 `s`, `m` or `h`. It remains the ceiling that bounds a hang: a process that has stopped producing is only caught by elapsed wall clock, never by a token count.",
                    "pattern": "^[0-9]+[smh]$",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "token_budget": {
                    "description": "Worker allowance denominated in generated output tokens \u2014 the quantity the same task needs regardless of what else the lane is doing, so a slow lane inside its token budget is not an overrun however long it took. Written as a bare integer of output tokens. Cannot bound a hang, so the wall-clock `time_budget` ceiling stays under its own name.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "unreconciled_run_grace": {
                    "description": "How long a complete or blocked worker manifest may remain as a live pointer before dispatch refuses more work for that project.",
                    "pattern": "^[0-9]+[smh]$",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "FenceConfig",
            "type": "object"
        },
        "FlightConfig": {
            "additionalProperties": false,
            "description": "A complete flight configuration, or one layer of one.",
            "properties": {
                "backends": {
                    "additionalProperties": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/BackendConfig__identifier_optional"
                            },
                            {
                                "type": "null"
                            }
                        ]
                    },
                    "description": "Available worker backends, keyed by a name chosen by whoever writes the configuration. The schema fixes no backend names.",
                    "type": [
                        "object",
                        "null"
                    ]
                },
                "budget": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/BudgetConfig"
                        },
                        {
                            "type": "null"
                        }
                    ]
                },
                "default_backend": {
                    "description": "Name of the backend used when a role does not select one. Must name a key of `backends` once every layer has been merged; a name with no backend behind it is a configuration error rather than an implicit fallback.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "fences": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/FenceConfig"
                        },
                        {
                            "type": "null"
                        }
                    ]
                },
                "gates": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/GateConfig"
                        },
                        {
                            "type": "null"
                        }
                    ]
                },
                "local_backend": {
                    "description": "Name of the locally served backend selected by `reckon crew dispatch --local`. Must name a key of `backends` once every layer has been merged. Absent means this host has no declared local worker route.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "review_excluded_backends": {
                    "description": "Names of backends that review routing must never select, whatever lane composes the review and whatever backend has already dropped the dispatch. A review fallback walks a list of configured backends, so an exclusion it can step around is a note rather than a rule; naming a backend here removes it from every composed review lane.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "roles": {
                    "additionalProperties": {
                        "anyOf": [
                            {
                                "$ref": "#/$defs/RoleConfig__identifier_optional"
                            },
                            {
                                "type": "null"
                            }
                        ]
                    },
                    "description": "Per-role routing overlays, keyed by role name. A role overrides only the keys it names; everything else falls through to its backend.",
                    "type": [
                        "object",
                        "null"
                    ]
                },
                "summary": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/SummaryConfig"
                        },
                        {
                            "type": "null"
                        }
                    ]
                },
                "ticker": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/TickerConfig"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "The follower pane's own memory of what it has rendered. A reader tracking a fleet across re-arms wants the rows it just saw restored rather than an empty pane; these bounds decide how much of the view comes back."
                },
                "version": {
                    "description": "Schema version of this configuration document.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "worktree": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/WorktreeConfig"
                        },
                        {
                            "type": "null"
                        }
                    ]
                }
            },
            "title": "FlightConfig",
            "type": "object"
        },
        "GateConfig": {
            "additionalProperties": false,
            "description": "How evidence gates are enforced.",
            "properties": {
                "enforce": {
                    "$ref": "#/$defs/GateEnforcement"
                },
                "on_fail": {
                    "$ref": "#/$defs/GateFailureAction"
                },
                "require_evidence": {
                    "description": "Whether a gate must produce recorded evidence to be considered met.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "suite_command": {
                    "description": "Project test-suite command whose presence arms promotion consequence checks. Absent means the project is unarmed; no command is supplied by shipped defaults.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "GateConfig",
            "type": "object"
        },
        "GateEnforcement": {
            "description": "How strictly evidence gates are applied.",
            "enum": [
                "strict",
                "advisory",
                "disabled"
            ],
            "title": "GateEnforcement",
            "type": "string"
        },
        "GateFailureAction": {
            "description": "What happens to downstream work when a gate fails.",
            "enum": [
                "hold",
                "warn",
                "continue"
            ],
            "title": "GateFailureAction",
            "type": "string"
        },
        "InputModality": {
            "description": "A kind of input a backend's model actually receives beside the prompt. Closed structural vocabulary: a value names an input channel, never a provider, a model or an effort level.",
            "enum": [
                "text",
                "image"
            ],
            "title": "InputModality",
            "type": "string"
        },
        "LaunchMode": {
            "description": "How a worker process for a backend is started.",
            "enum": [
                "cli",
                "in-harness"
            ],
            "title": "LaunchMode",
            "type": "string"
        },
        "PlacementConfig": {
            "additionalProperties": false,
            "description": "A scheduler wrapper declared for one backend's workers. It describes how the launch is wrapped and never what runs: the resolved command stays the backend's own, so a placement moves a worker between hosts without changing which lane serves it. It also declares what the worker needs visible where it lands, and how to ask the wrapper about a job it started.",
            "properties": {
                "job_id_probe": {
                    "description": "Argument vector asked which job the launch became, with `{run}` replaced by the run id. Read rather than guessed, and declared per backend because reckon knows no scheduler's own vocabulary; a probe that answers no identifier records why instead of a fabricated id.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "options": {
                    "description": "Argument strings passed to the scheduler ahead of the resolved command, such as a partition and a resource request. User data; the schema names no partition and no site.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "reason_query": {
                    "description": "Argument vector that asks the scheduler for one job's own reason string, with `{job}` replaced by the job id. A job that never started reports why here rather than through an exit status the scheduler client never produced.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "requirements": {
                    "description": "Filesystem paths and network endpoints this placement's workers need visible from the node the scheduler places them on. Checked before launch, so a placement into a partition that cannot see one is refused while naming which, rather than failing later in a way that reads as a worker defect. A path on per-node storage is refused by name: it exists on the dispatcher and not where the worker runs, so it fails silently.",
                    "items": {
                        "$ref": "#/$defs/PlacementRequirement"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "scheduler": {
                    "description": "Scheduler executable the launch is wrapped in. Resolved against the same PATH the launch searches, so an unresolvable wrapper is refused before launch rather than dying at exec and leaving an empty stream that reads as a worker turn.",
                    "type": "string"
                },
                "state_query": {
                    "description": "Argument vector that asks the scheduler for one job's state, with `{job}` replaced by the job id. Declared beside the wrapper it asks, so which reporting verb answers a given scheduler is configuration rather than a table in reckon's own code, and a wrapper declared without one is a placement reckon cannot follow rather than one it silently cannot query.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                }
            },
            "required": [
                "scheduler"
            ],
            "title": "PlacementConfig",
            "type": "object"
        },
        "PlacementRequirement": {
            "additionalProperties": false,
            "description": "One filesystem path or network endpoint this placement's workers need visible from the node they run on. Exactly one of `path` and `endpoint` is declared; a requirement naming both or neither is a configuration error rather than a check that quietly passes.",
            "properties": {
                "endpoint": {
                    "description": "A network endpoint, given as host and port, this placement's workers must be able to reach \u2014 the served model and its router are the intended case.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "name": {
                    "description": "Map key for an inlined entry.",
                    "type": "string"
                },
                "path": {
                    "description": "A filesystem path this placement's workers must be able to see. User data; the schema names no site and fixes no layout.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "name"
            ],
            "title": "PlacementRequirement",
            "type": "object"
        },
        "RoleConfig": {
            "additionalProperties": false,
            "description": "A routing overlay for one kind of node. Every slot is optional; an unset slot inherits from the selected backend.",
            "properties": {
                "backend": {
                    "description": "Backend this role dispatches to. Absent means `default_backend`.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "by_spec_level": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/SpecificationRouting"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Routing overlays selected by the specification completeness declared for a node. An undeclared level applies no overlay."
                },
                "effort": {
                    "description": "Reasoning-effort level passed to this backend. Free text because each backend defines its own vocabulary, and because an effort ladder must not be fixed by reckon.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "execution_capable": {
                    "description": "Whether this role runs commands that can write build, test, cache or product state inside its detached worktree.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "model": {
                    "description": "Model identifier passed to this backend. User data; free text so that no provider vocabulary is encoded here.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "name": {
                    "description": "Map key for an inlined entry.",
                    "type": "string"
                },
                "sandbox": {
                    "$ref": "#/$defs/SandboxMode",
                    "description": "Filesystem blast radius granted to workers of this backend."
                },
                "session_reuse": {
                    "description": "Whether a finished worker session can be resumed rather than respawned.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "time_budget": {
                    "description": "Wall-clock allowance, written as an integer followed by a unit \u2014 `s`, `m` or `h`. It remains the ceiling that bounds a hang: a process that has stopped producing is only caught by elapsed wall clock, never by a token count.",
                    "pattern": "^[0-9]+[smh]$",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "token_budget": {
                    "description": "Worker allowance denominated in generated output tokens \u2014 the quantity the same task needs regardless of what else the lane is doing, so a slow lane inside its token budget is not an overrun however long it took. Written as a bare integer of output tokens. Cannot bound a hang, so the wall-clock `time_budget` ceiling stays under its own name.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "write_paths": {
                    "description": "Default write scope granted to a node of this role when it declares no write_paths of its own. Entries are relative and are resolved against the dispatching run's own durable report-and-log directory \u2014 the same directory `manifest_path` already defaults into \u2014 never against the repository being worked on. A shipped or host layer therefore names no host-specific location, and a role whose entries all stay under that directory grants no reach into repository source.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                }
            },
            "required": [
                "name"
            ],
            "title": "RoleConfig",
            "type": "object"
        },
        "RoleConfig__identifier_optional": {
            "additionalProperties": false,
            "description": "A routing overlay for one kind of node. Every slot is optional; an unset slot inherits from the selected backend.",
            "properties": {
                "backend": {
                    "description": "Backend this role dispatches to. Absent means `default_backend`.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "by_spec_level": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/SpecificationRouting"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Routing overlays selected by the specification completeness declared for a node. An undeclared level applies no overlay."
                },
                "effort": {
                    "description": "Reasoning-effort level passed to this backend. Free text because each backend defines its own vocabulary, and because an effort ladder must not be fixed by reckon.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "execution_capable": {
                    "description": "Whether this role runs commands that can write build, test, cache or product state inside its detached worktree.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "model": {
                    "description": "Model identifier passed to this backend. User data; free text so that no provider vocabulary is encoded here.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "name": {
                    "description": "Map key for an inlined entry.",
                    "type": "string"
                },
                "sandbox": {
                    "$ref": "#/$defs/SandboxMode",
                    "description": "Filesystem blast radius granted to workers of this backend."
                },
                "session_reuse": {
                    "description": "Whether a finished worker session can be resumed rather than respawned.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "time_budget": {
                    "description": "Wall-clock allowance, written as an integer followed by a unit \u2014 `s`, `m` or `h`. It remains the ceiling that bounds a hang: a process that has stopped producing is only caught by elapsed wall clock, never by a token count.",
                    "pattern": "^[0-9]+[smh]$",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "token_budget": {
                    "description": "Worker allowance denominated in generated output tokens \u2014 the quantity the same task needs regardless of what else the lane is doing, so a slow lane inside its token budget is not an overrun however long it took. Written as a bare integer of output tokens. Cannot bound a hang, so the wall-clock `time_budget` ceiling stays under its own name.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "write_paths": {
                    "description": "Default write scope granted to a node of this role when it declares no write_paths of its own. Entries are relative and are resolved against the dispatching run's own durable report-and-log directory \u2014 the same directory `manifest_path` already defaults into \u2014 never against the repository being worked on. A shipped or host layer therefore names no host-specific location, and a role whose entries all stay under that directory grants no reach into repository source.",
                    "items": {
                        "type": "string"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                }
            },
            "required": [],
            "title": "RoleConfig",
            "type": "object"
        },
        "RoutingOverlay": {
            "additionalProperties": false,
            "description": "Settings that replace the selected role and backend routing for one level.",
            "properties": {
                "backend": {
                    "description": "Backend this role dispatches to. Absent means `default_backend`.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "effort": {
                    "description": "Reasoning-effort level passed to this backend. Free text because each backend defines its own vocabulary, and because an effort ladder must not be fixed by reckon.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "model": {
                    "description": "Model identifier passed to this backend. User data; free text so that no provider vocabulary is encoded here.",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "time_budget": {
                    "description": "Wall-clock allowance, written as an integer followed by a unit \u2014 `s`, `m` or `h`. It remains the ceiling that bounds a hang: a process that has stopped producing is only caught by elapsed wall clock, never by a token count.",
                    "pattern": "^[0-9]+[smh]$",
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "token_budget": {
                    "description": "Worker allowance denominated in generated output tokens \u2014 the quantity the same task needs regardless of what else the lane is doing, so a slow lane inside its token budget is not an overrun however long it took. Written as a bare integer of output tokens. Cannot bound a hang, so the wall-clock `time_budget` ceiling stays under its own name.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                }
            },
            "title": "RoutingOverlay",
            "type": "object"
        },
        "SandboxMode": {
            "description": "The filesystem blast radius granted to a worker.",
            "enum": [
                "read-only",
                "workspace-write",
                "worktree-full"
            ],
            "title": "SandboxMode",
            "type": "string"
        },
        "SpecificationRouting": {
            "additionalProperties": false,
            "description": "Routing overlays keyed by the closed specification-level vocabulary.",
            "properties": {
                "exact": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/RoutingOverlay"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Routing for a node whose implementation is fully prescribed."
                },
                "guided": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/RoutingOverlay"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Routing for a node whose design is fixed but implementation is derived."
                },
                "open": {
                    "anyOf": [
                        {
                            "$ref": "#/$defs/RoutingOverlay"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "description": "Routing for a node whose design and implementation remain to the worker."
                }
            },
            "title": "SpecificationRouting",
            "type": "object"
        },
        "SummaryConfig": {
            "additionalProperties": false,
            "description": "When and how workers report.",
            "properties": {
                "at": {
                    "description": "Occasions on which a worker emits a summary.",
                    "items": {
                        "$ref": "#/$defs/SummaryOccasion"
                    },
                    "type": [
                        "array",
                        "null"
                    ]
                },
                "reflex": {
                    "description": "The reporting shape a worker follows when it summarises.",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "SummaryConfig",
            "type": "object"
        },
        "SummaryOccasion": {
            "description": "A point in a worker's life at which it reports.",
            "enum": [
                "dispatch",
                "completion",
                "micro-plan",
                "hold"
            ],
            "title": "SummaryOccasion",
            "type": "string"
        },
        "TickerConfig": {
            "additionalProperties": false,
            "description": "Bounds on the follower pane's own memory. The follower keeps the rows it rendered so a re-arm can restore a reader's view rather than opening empty; the caps decide how much of that view comes back.",
            "properties": {
                "history_rows": {
                    "description": "Most rows a follower re-emits as history when it re-arms. A pane restored from a long outage should show recent work rather than the whole session, so the cap counts rendered rows and is applied with `history_window`, whichever admits fewer.",
                    "minimum": 1,
                    "type": [
                        "integer",
                        "null"
                    ]
                },
                "history_window": {
                    "description": "Oldest history row a follower re-emits when it re-arms, written as an integer followed by a unit \u2014 `s`, `m` or `h`. Trimmed against `history_rows`, whichever admits fewer, so a quiet session's short log is not replayed in full after a long gap.",
                    "pattern": "^[0-9]+[smh]$",
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "title": "TickerConfig",
            "type": "object"
        },
        "WorktreeCleanup": {
            "description": "How aggressively finished worktrees are removed.",
            "enum": [
                "conservative",
                "force",
                "never"
            ],
            "title": "WorktreeCleanup",
            "type": "string"
        },
        "WorktreeConfig": {
            "additionalProperties": false,
            "description": "Worktree lifecycle policy.",
            "properties": {
                "cleanup": {
                    "$ref": "#/$defs/WorktreeCleanup"
                }
            },
            "title": "WorktreeConfig",
            "type": "object"
        }
    },
    "$id": "https://reckon/schema/flight",
    "$schema": "https://json-schema.org/draft/2019-09/schema",
    "additionalProperties": false,
    "description": "A complete flight configuration, or one layer of one.",
    "metamodel_version": "1.11.0",
    "properties": {
        "backends": {
            "additionalProperties": {
                "anyOf": [
                    {
                        "$ref": "#/$defs/BackendConfig__identifier_optional"
                    },
                    {
                        "type": "null"
                    }
                ]
            },
            "description": "Available worker backends, keyed by a name chosen by whoever writes the configuration. The schema fixes no backend names.",
            "type": [
                "object",
                "null"
            ]
        },
        "budget": {
            "anyOf": [
                {
                    "$ref": "#/$defs/BudgetConfig"
                },
                {
                    "type": "null"
                }
            ]
        },
        "default_backend": {
            "description": "Name of the backend used when a role does not select one. Must name a key of `backends` once every layer has been merged; a name with no backend behind it is a configuration error rather than an implicit fallback.",
            "type": [
                "string",
                "null"
            ]
        },
        "fences": {
            "anyOf": [
                {
                    "$ref": "#/$defs/FenceConfig"
                },
                {
                    "type": "null"
                }
            ]
        },
        "gates": {
            "anyOf": [
                {
                    "$ref": "#/$defs/GateConfig"
                },
                {
                    "type": "null"
                }
            ]
        },
        "local_backend": {
            "description": "Name of the locally served backend selected by `reckon crew dispatch --local`. Must name a key of `backends` once every layer has been merged. Absent means this host has no declared local worker route.",
            "type": [
                "string",
                "null"
            ]
        },
        "review_excluded_backends": {
            "description": "Names of backends that review routing must never select, whatever lane composes the review and whatever backend has already dropped the dispatch. A review fallback walks a list of configured backends, so an exclusion it can step around is a note rather than a rule; naming a backend here removes it from every composed review lane.",
            "items": {
                "type": "string"
            },
            "type": [
                "array",
                "null"
            ]
        },
        "roles": {
            "additionalProperties": {
                "anyOf": [
                    {
                        "$ref": "#/$defs/RoleConfig__identifier_optional"
                    },
                    {
                        "type": "null"
                    }
                ]
            },
            "description": "Per-role routing overlays, keyed by role name. A role overrides only the keys it names; everything else falls through to its backend.",
            "type": [
                "object",
                "null"
            ]
        },
        "summary": {
            "anyOf": [
                {
                    "$ref": "#/$defs/SummaryConfig"
                },
                {
                    "type": "null"
                }
            ]
        },
        "ticker": {
            "anyOf": [
                {
                    "$ref": "#/$defs/TickerConfig"
                },
                {
                    "type": "null"
                }
            ],
            "description": "The follower pane's own memory of what it has rendered. A reader tracking a fleet across re-arms wants the rows it just saw restored rather than an empty pane; these bounds decide how much of the view comes back."
        },
        "version": {
            "description": "Schema version of this configuration document.",
            "minimum": 1,
            "type": [
                "integer",
                "null"
            ]
        },
        "worktree": {
            "anyOf": [
                {
                    "$ref": "#/$defs/WorktreeConfig"
                },
                {
                    "type": "null"
                }
            ]
        }
    },
    "title": "reckon-flight",
    "type": "object",
    "version": null
}

