This merge commit also carried docs/plans/background-agent-fitness-and-dispatch-scope.html, which belongs to neither side of the merge and is not described by its message.

The file was staged in the shared checkout by a concurrent coordinator session when this merge concluded, so the merge commit took it from the index. Its 43 added lines record the nova lane-fitness result: on the classes where both lanes have rows the defect rates do not separate, so routing rules key to the kind of work and its gate rather than to the backend.

The content is correct and intended; only its attribution is wrong. Recorded here rather than rewritten, because the commit is already pushed.
