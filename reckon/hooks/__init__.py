"""Hook scripts a harness runs at a tool boundary.

Each module here is designed to be copied standalone into a target
repository's harness settings — see ``native_agent_guard`` for the
self-contained convention every hook in this package follows.
"""
